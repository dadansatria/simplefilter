# Simple Filter for Laravel Eloquent

A simple and flexible filtering trait for Laravel Eloquent models.

## Installation

You can install the package via composer.

``` composer require dadansatria/simplefilter ```

